import { BigNumber } from 'ethers'

export const DEFAULT_CHAIN_ID: BigNumber = BigNumber.from(31337) // Hardhat default chain ID
export const DEFAULT_SERIAL_NUMBER: BigNumber = BigNumber.from(0)
export const DEFAULT_PREVIOUS_TOKEN_ID: BigNumber = BigNumber.from(0)
export const DEFAULT_TOKEN_NAME = 'Test'
export const DEFAULT_TOKEN_SYMBOL = 'TST'

export const DEFAULT_MAX_TOKEN_INDEX: BigNumber = BigNumber.from(100)
